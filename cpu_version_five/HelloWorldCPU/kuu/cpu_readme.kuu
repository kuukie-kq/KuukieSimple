只有无符号整数类型
可表示数字的位数只有15位，既最大全1，32767，最小全0，0
注意（如果为0再相减，则32位有符号表示为-1）既原码形式的-1（11111111111111111111111111111111）（4294967295）

//
add函数
两个参数
做加法运算
如果第二个参数为0，则直接返回
返回值为两数相加的结果
文件长度10行
//

//
gather函数
两个参数
做比较运算
前半段做有0的时候的判断，后半段循环相减再判断
返回值为1第一个数大，2第二个数大，0两数相等
文件长度22行
//

//
seb函数
两个参数
做减法运算
一定是大减小，且如果第二个参数为0，则直接返回
返回值为两数相减的结果
文件长度10行
//